name: CI/CD BATISTA PORTFOLIO

on:
  push: 
    branches: 
      - main

jobs: 
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm test
      - run: npm build
          
